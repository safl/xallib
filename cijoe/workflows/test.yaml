---
doc: |
  Invoke the testrunner

  This assumes that you have already setup the block-device and file-system used
  for testing.

steps:
- name: test
  uses: core.testrunner
  with:
    run_local: true
    random_order: false
    args: "tests"
